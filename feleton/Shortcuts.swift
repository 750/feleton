import KeyboardShortcuts

extension KeyboardShortcuts.Name {
  static let toggleFeleton = Self("toggleFeleton", default: Shortcut(.f4))
  
//  static let openSettings = Self("openSettings", default: Shortcut(.comma, modifiers: .command))
}
